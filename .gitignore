# desktop build artifacts
app/app/
dist/
app/embed.ts
*.so
*.AppImage
*.app/
*.dmg
*.deb
*.rpm
*.msi
*.snap

# frontend build + deps
app/web/dist/
app/web/dist-link/
hub/api/link-embed.ts
node_modules/

# e2e artifacts
app/web/test-results/
app/web/playwright-report/

# local runtime state / secrets
.env
.scratch/
hub/certs/
hub/ca/

# playwright MCP page snapshots
.playwright-mcp/

# root-level debug/demo screenshots (real docs images live in docs/)
/*.jpeg
/*.png

# docs site (Astro + Starlight)
docs-site/node_modules/
docs-site/dist/
docs-site/.astro/

# local AI-tool config
/.agents/
/.codex/

# personal Trame pre-push guard (core.hooksPath is .githooks, so it must live there)
/.githooks/pre-push
